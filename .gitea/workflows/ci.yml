name: CI
on:
  workflow_dispatch:
  push:
    branches: [main]
jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo CHECKOUT_NO_SUBMODULE_OK && ls -d .gitea
      - name: pwsh check
        shell: pwsh
        run: 'Write-Host "PWSH_OK"'
