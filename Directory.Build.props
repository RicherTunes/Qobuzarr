<Project>
  <PropertyGroup>
    <!-- Host (Lidarr) targets net6. Suppress SDK EOL warning NETSDK1138 during local builds. -->
    <CheckEolTargetFramework>false</CheckEolTargetFramework>
  </PropertyGroup>
</Project>
