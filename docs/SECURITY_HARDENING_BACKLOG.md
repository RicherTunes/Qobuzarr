# Security Hardening Backlog — qobuzarr

_Generated: 2026-05-23 | Phase 1 security audit_

| ID | Dimension | Finding | Severity | File:Line | Recommendation | Tracking-issue-ref |
|----|-----------|---------|----------|-----------|---------------|-------------------|
| QOB-001 | Auth & token storage | **Three sync-over-async calls inside `lock {}` blocks** in `QobuzAuthenticationService`: `TryLoadPersistedSession` (line 502), `TryPersistSession` (line 526), and `TryClearPersistedSession` (line 546). Acknowledged by source-level TODO comments referencing `docs/SYNC_ASYNC_DEBT.md`. On a context where the thread pool is saturated these can deadlock because `GetAwaiter().GetResult()` inside a `lock` cannot yield. | High | `src/Authentication/QobuzAuthenticationService.cs:502,526,546` | Implement the documented TODO (Phase 1.1): make `IQobuzAuthenticationService` async; propagate `async Task` through `GetCachedSession`, `StoreSession`, `ClearSession`. Use `SemaphoreSlim` instead of `lock` in the async path. | QOB-001 |
| QOB-002 | Auth & token storage | `QobuzDownloadClient.cs:1019` calls `DisposeAsync().ConfigureAwait(false).GetAwaiter().GetResult()` inside `Dispose()`. This pattern is documented as problematic (Category B sync-over-async debt). It can throw `InvalidOperationException` if Lidarr disposes the client from a non-threadpool context. | Medium | `src/Download/Clients/QobuzDownloadClient.cs:1019` | Implement `IAsyncDisposable` and let Lidarr call `DisposeAsync`; the synchronous `Dispose()` wrapper should call `Task.Run(() => DisposeAsync().AsTask()).Wait()` with a thread-pool hop to avoid context capture. | QOB-002 |
| QOB-003 | API key handling | `QobuzIndexerSettings.AppSecret` is stored as a plaintext `string` property with `FieldType.Textbox` (not `Password`) in the Lidarr UI (line 85). The App Secret is a sensitive value if a user supplies their own Qobuz app credentials; it appears in plaintext in the settings JSON file. | High | `src/Indexers/QobuzIndexerSettings.cs:85` | Change the field type to `FieldType.Password` / `Privacy = PrivacyLevel.Password`. Additionally, store the secret through Common's `TokenProtectorFactory` when a user-supplied secret is present. | QOB-003 |
| QOB-004 | Hardcoded constants | `QobuzConstants.Authentication.DefaultAppId` is `"798273057"` (line 33 of `QobuzConstants.cs`). This is the public Qobuz web-player app ID extracted from the web player source. It is a public constant, not a private secret, and has been widely published in open-source Qobuz clients. No ADR exists documenting this status. | Medium | `src/Configuration/QobuzConstants.cs:33` | Create `docs/decisions/0001-hardcoded-qobuz-app-id.md` using tidalarr's `0001-hardcoded-tidal-client-credentials.md` as a template. Add the constant to `.gitleaks.toml` allowlist. | QOB-004 |
| QOB-005 | Path & file validation | `FilePathGenerator.GenerateOptimizedFileName` delegates to `Sanitize.PathSegment` (Common) for artist and title (lines 34–35), which is correct. However, the generated `trackNumber` string (`QobuzPluginConstants.FileNaming.TrackNumberFormat`) is not sanitized and is prepended directly to the filename. A malformed track number containing `/` or `\` would produce a path traversal. | Medium | `src/Download/Services/FilePathGenerator.cs:36-38` | Pass `trackNumber` through `Sanitize.PathSegment` before interpolation; or assert it matches `^\d+$` at the point of use. | QOB-005 |
| QOB-006 | Path & file validation | `DownloadFileService.cs:259` sanitizes the filename using `Sanitize.PathSegment`, but the **directory path** component (artist/album folder) created by the download orchestrator uses a different code path. Verify that folder-name components are also routed through `Sanitize.PathSegment`. | Medium | `src/Download/Services/DownloadFileService.cs:259` | Audit the full path construction chain in `DownloadOrchestrator` to confirm all path segments (artist, album, year) pass through `Sanitize.PathSegment` before `Path.Combine`. | QOB-006 |
| QOB-007 | Sensitive logging | `BridgeQobuzAuthHelper.cs:91` logs `email` at `Debug` level: `"Bridge auth: attempting login for {Email}"`. Email addresses are PII. Similarly line 124 logs `UserId` and subscription `Tier`. These appear in Lidarr's debug log which may be shared in support tickets. | Medium | `src/Integration/Bridge/BridgeQobuzAuthHelper.cs:91,124` | Redact or hash the email before logging (e.g., log only the domain part). Log `UserId` as a truncated or hashed value. | QOB-007 |
| QOB-008 | Auth & token storage | `QobuzIndexerAdapter.cs:66` calls `async` lambda via `.AsTask().GetAwaiter().GetResult()` in what appears to be Lidarr's sync `Fetch()` contract. This is a Category A sync-over-async pattern — forced by a sync Lidarr interface. | Medium | `src/Integration/QobuzIndexerAdapter.cs:66` | Add an explicit comment citing the Category A rationale. Ensure the lambda is wrapped in `Task.Run` before `.GetAwaiter().GetResult()` to guarantee a thread-pool context hop. | QOB-008 |
| QOB-009 | Retry abuse / DoS | `QobuzConstants.Api.MaxRetries = GlobalMaxRetryAttempts` (from `QobuzarrConstants`). Without inspecting `QobuzarrConstants.Defaults.GlobalMaxRetryAttempts` we cannot confirm the value, but if it is ≥ 5 and requests are made under no per-host semaphore, a failed Qobuz server could trigger a burst of retries across all concurrent callers. | Medium | `src/Configuration/QobuzConstants.cs:34` | Verify `GlobalMaxRetryAttempts` value; cap at 3 for unauthenticated requests. Ensure `ExponentialBackoffRetryPolicy` (Common) is the implementation used — not a bespoke retry loop. | QOB-009 |
| QOB-010 | Network failure behaviour | `LegacySessionMigrator.cs:62` calls `.GetAwaiter().GetResult()` during migration. If the migration is triggered during a network partition the blocking call can hold a lock or thread-pool thread for the full HTTP timeout (default 30 s in `QobuzConstants.Api.RequestTimeoutSeconds`). | Low | `src/Authentication/LegacySessionMigrator.cs:62` | Run migration in a `Task.Run` background task with a short timeout; if migration fails, leave legacy file in place and retry on next startup rather than blocking the calling thread. | QOB-010 |
| QOB-011 | Sensitive logging | `QobuzAuthenticationService.cs` creates a static `RawHttpClient` and `WebPlayerHttpClient` (lines 50–51). These clients are not registered with Lidarr's HTTP logging pipeline that applies `Sanitize.RedactUrls`. Any connection errors logged by these clients will include raw query strings potentially containing app IDs/secrets. | Medium | `src/Authentication/QobuzAuthenticationService.cs:50-51` | Register these clients through Common's `PluginHttpOptions` / `OAuthDelegatingHandler` pipeline, or apply a custom `DelegatingHandler` that redacts URIs in exceptions before they propagate to the logger. | QOB-011 |
